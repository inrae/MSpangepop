conda create -n wf_env -c conda-forge -c bioconda snakemake=8.4.7

# Run this to update the dependencies box, temporary image for testing

docker build -t msprime_box ./temporary_dependencies/ && snakemake build ./temporary_dependencies/msprime_box.sif docker-daemon://msprime_box:latest